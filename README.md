# talent-match-engine
A scalable microservices platform for predicting the match between candidates and job openings. Combines feature stores, transformer-based CV embeddings, and ML models to generate match scores in real-time.
